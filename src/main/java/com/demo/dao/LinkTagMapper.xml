<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dao.LinkTagMapper">

    <select id="selectCountByKey" parameterType="com.demo.pojo.LinkTag" resultType="int">
        select count(*) from link_tag where account=#{account} and name=#{name};
    </select>

    <insert id="insertTag">
    </insert>
</mapper>