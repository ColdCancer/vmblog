<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dao.ArticleMapper">

    <select id="selectByAccount" resultType="com.demo.pojo.Article">
        select * from article where account=#{account};
    </select>

    <select id="selectFirstPostArticle" resultType="com.demo.pojo.Article">
        select * from article order by post_time desc limit 1;
    </select>

    <insert id="insertArticle" parameterType="com.demo.pojo.Article">
        insert into article(id, account, title, post_time)
         values (#{id}, #{account}, #{title}, #{post_time});
    </insert>

    <select id="countByAccount" resultType="java.lang.Integer">
        select count(*) from article where account=#{account};
    </select>

    <select id="countByIdAndAccount" resultType="java.lang.Integer">
        select count(*) from article where id=#{id} and account=#{account};
    </select>
</mapper>